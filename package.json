{
  "name": "flatfinderil",
  "private": true,
  "scripts": {
    "dev:miniapp": "pnpm --filter miniapp dev",
    "dev:web": "pnpm --filter web dev",
    "build": "turbo build",
    "build:miniapp": "turbo build --filter=miniapp",
    "build:web": "turbo build --filter=web",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
